config = dict(
    # Path to txflow logs
    TXFLOW_LOGS = 'example/',

    # Number of epochs to display (only tail is displayed)
    DISPLAY_EPOCHS_BLOCKS = 10,

    # Watch logs folder on real time and update current information if needed
    WATCH_REAL_TIME = True,
)